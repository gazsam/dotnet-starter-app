applications:
- name: dotnet-starter
  mem: 512M
  services:
    ${name}-sqlserver14-db:
      type: mssql2014
	${name}-mysql-db:
      type: mysql
	${name}-postgres-db:
      type: postgresql
	${name}-rabbitmq:
      type: rabbit
	${name}-rabbitmq3:
      type: rabbit3
	${name}-mongo-db:
      type: mongodb
	${name}-filesystem:
      type: filesystem
	${name}-harbor:
      type: harbor
	${name}-memcached:
      type: memcached
	${name}-sqlserver12-db:
      type: mssql2012